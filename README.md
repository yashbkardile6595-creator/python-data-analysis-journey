 Python Data Analysis 


📚 Learning 

✅ NumPy : Basics
I practiced the fundamentals of NumPy arrays.

Topics covered:

* Introduction to NumPy
* Creating NumPy arrays
* 1D and 2D arrays
* Array `shape`
* Array `ndim`
* Array `size`
* Array indexing
* Array slicing
* 2D array indexing
* Working with rows and columns

Notebook:  NumPy Basics.ipynb`

✅ NumPy — Day 2: Operations

Topics covered:

 Arithmetic operations
 NumPy array operations
 Universal Functions (ufunc)

  * `sqrt()`
  * `square()`
  * `abs()`
  * `round()`
* Aggregation functions

  * `sum()`
  * `mean()`
  * `min()`
  * `max()`
* Reshaping arrays
* Flattening arrays
* Boolean masking
* Random number generation
* `random.rand()`
* `random.randint()`
* `random.choice()`
* Broadcasting
* Working with sales data
* Practical NumPy assignments

Notebook:  NumPy Operations.ipynb`



 ✅ Pandas — Data Manipulation

Topics covered:

* Importing Pandas
* Reading CSV datasets
* Working with DataFrames
* Titanic dataset analysis
* Detecting missing values
* Handling missing values
* `isna()`
* `dropna()`
* `fillna()`
* `bfill()`
* `ffill()`
* Calculating mean values
* Detecting duplicate values
* Checking DataFrame columns
* Sorting data
* Sorting by multiple columns
* Tips dataset analysis
* Random sampling
* Descriptive statistics using `describe()`
* `value_counts()`
* `nlargest()`
* `nsmallest()`
* `groupby()`
* Calculating group-wise sum and mean
* Filtering data
* `between()`
* Selecting data using `loc[]`

Notebook: `Data Manipulation with Pandas.ipynb`


## 📂 Repository Structure

```text
python-data-analysis-journey/
│
├── NumPy/
│   ├── Day 1 NumPy Basics.ipynb
│   └── Day 2 NumPy Operations.ipynb
│
├── Pandas/
│   └── Data Manipulation with Pandas.ipynb
│
└── README.md


 

* Python
* NumPy
* Pandas
* Jupyter Notebook


